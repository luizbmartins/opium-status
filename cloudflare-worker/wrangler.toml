name = "opium-status-cron"
main = "src/index.js"
compatibility_date = "2026-09-13"

[triggers]
crons = ["*/5 * * * *"]

# Secret necessario (nao vai neste arquivo):
#   npx wrangler secret put GH_DISPATCH_TOKEN
# PAT fine-grained do GitHub, escopo so no repo opium-status,
# permissao "Contents: Read and write".
